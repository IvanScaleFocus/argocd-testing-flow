apiVersion: v1
kind: Service
metadata:
  name: argocd-myapp-expose-service

spec:
  type: NodePort
  ports:
   - targetPort: 80
     port: 8080
     nodePort: 30008
     protocol: TCP
  selector:
    app: myapp
